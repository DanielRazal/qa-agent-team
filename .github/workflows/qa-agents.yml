name: QA Agent Team

on:
  workflow_dispatch:
    inputs:
      url:
        # practicesoftwaretesting.com serves a Cloudflare challenge to CI runners
        description: Application to explore
        default: https://demo.playwright.dev/todomvc
      pages:
        description: Max pages to crawl
        default: "3"
  schedule:
    - cron: "0 3 * * *" # nightly, catches UI drift overnight

jobs:
  explore-design-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          python -m playwright install --with-deps chromium

      - name: Run the agent pipeline
        id: pipeline
        continue-on-error: true # test failures are triaged, not fatal
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python main.py "${{ inputs.url || 'https://demo.playwright.dev/todomvc' }}" \
            --pages "${{ inputs.pages || 3 }}" --per-page 5 --triage

      - name: Write job summary
        if: always()
        run: python ci_summary.py >> "$GITHUB_STEP_SUMMARY"

      - name: Upload generated suite and reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: qa-agent-output
          path: |
            output/
            tests_generated/
          retention-days: 14
          if-no-files-found: ignore # the pipeline may have died before writing anything

      - name: Fail if anything other than the app is broken
        if: always()
        run: python ci_gate.py
